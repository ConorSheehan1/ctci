# use circular array to maximize space usage (if one stack is empty allow other stack to fill space
# including wrapping last stack around to front of array)
# keep track of head of each stack, never allow heads to overlap